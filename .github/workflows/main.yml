name: Docker Build & Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: webpronest:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Ensure Docker network exists
        run: |
          docker network inspect webpronest-network >/dev/null 2>&1 \
            || docker network create webpronest-network

      - name: Start container
        run: |
          docker run -d --name app_test -p 3000:3000 webpronest:${{ github.sha }}
          docker inspect --format '{{json .State.Health}}' app_test

      # 🔥 ADD THIS HEALTH CHECK STEP
      - name: Health check
        run: |
          echo "Waiting for application to become healthy..."
          for i in {1..40}; do
            STATUS=$(docker inspect --format='{{.State.Health.Status}}' app_test || echo "starting")

            echo "Current status: $STATUS"

            if [ "$STATUS" = "healthy" ]; then
              echo "Container is healthy!"
              exit 0
            fi

            echo "Health not healthy yet... "
            echo "Showing docker logs"
            docker logs app_test
            echo "retrying ($i/40)"
            sleep 3
          done

          echo "Container failed health check!"
          docker logs app_test
          exit 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            # Exit on any error
            set -e

            # Navigate to project directory
            cd ${{ secrets.PROJECT_PATH }}

            # Ensure we're on the desired branch and up-to-date
            git fetch --all
            git reset --hard origin/main

            # Ensure Docker network exists
            docker network inspect webpronest-network >/dev/null 2>&1 \
              || docker network create webpronest-network
            
            # Pull latest changes and redeploy
            docker compose --profile prod down
            docker compose --profile prod up --build -d webpronest-frontend-prod

            # Clean up unused Docker resources
            docker system prune -a -f
